<h1>Editing policy</h1>

<%= nested_form_for(@policy) do |f| %>
  <%= render 'form', :form => f, :policy => @policy %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<%= link_to 'Show', @policy %> |
<%= link_to 'Back', policies_path %>
