<h1>Articles</h1>

<%= render :partial => 'article', :collection => @articles, :locals => {:summary => true} %>

<br />

<%= link_to 'New Article', new_article_path if current_user.can?(:create, :articles)%>

<%= pagination(Article, @page, @per_page) -%>
